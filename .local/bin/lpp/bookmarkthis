#!/bin/sh

#credit: Luke Smith

bookmark=$(xclip -o)
file="$HOME/.local/share/larbs/snippets.csv"


if grep -q "^$bookmark$" "$file"; then
	notify-send "Already bookmarked"
else
	notify-send "Bookmark added: $bookmark"
	echo "$bookmark" >> "$file"
fi
